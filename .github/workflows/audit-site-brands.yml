name: Audit site brands

on:
  push:
    paths:
      - 'tools/audit_site_brands.py'
      - '.github/workflows/audit-site-brands.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python tools/audit_site_brands.py
      - name: Commit audit report
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add site-brand-audit.md
          git diff --cached --quiet || git commit -m "Report site brand coverage"
          git push
